function compareNumbers(a,b){
    return a===b? a*b: a+b;
}
console.log(compareNumbers(2,2));
console.log(compareNumbers(1,2));
// on running it, it should return the square root(4) first and then the sum(3)last. 